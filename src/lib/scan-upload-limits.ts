/**
 * スキャンのアップロード受付上限 — **唯一の真実源**。
 *
 * この値はクライアント (`src/scripts/scan-upload.ts` の受付判定) と
 * サーバの S3 チケット発行 (`src/lib/scan-upload-ticket.ts` の署名上限) の
 * **両方で使う**。以前は 2 ファイルに別々のリテラルで持っていたため、
 * 2026-09-17 にクライアントを 10 MB → 40 MB へ上げたとき
 * (`06d4375`) **チケット側 (10 MB) の上げ忘れ**が起き、10 MB 超の
 * ファイルが S3 直アップロードに乗れず 3.2 MB の圧縮フォールバックへ
 * 落ちる (= 実質 3 MB 制限に見える) 回帰を招いた。
 *
 * → ここ 1 か所に集約し、両者が構造的に食い違わないようにする。
 *
 * **なぜ 40 MB か**: ファイル本体は関数を通らず **ブラウザ → S3 直 PUT** で
 * 置くので、Vercel のリクエストボディ 4.5 MB 制限を受けない (同方式を LAiF
 * ポータルが 50 MB で運用)。複数年の人間ドック PDF は 10 MB では詰まる
 * (発注者指示 2026-09-17)。S3 が使えない回だけ `WIRE_BUDGET_BYTES`
 * (≈3.2 MB) の圧縮フォールバックになる (`scan-upload.ts` の経路③)。
 */
export const SCAN_UPLOAD_MAX_BYTES = 40 * 1024 * 1024;
