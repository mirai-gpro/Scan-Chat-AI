# トランスコスモス10名 ― 一括投入 仕様書 v4.0

作成: 2026-09-15 / 更新: 2026-09-15 (Q1〜Q4 / Q7〜Q10 裁定 + 実物 ZIP の実測 §15 + **変換表 §16 確定**)
状態: **設計。発注者承認まで実装しない**
撤回: `design/transcos-zero-base` (Scan-Chat-AI `7d30669` / wellfort-site `146fa46`)

---

## 0. この案件は何か

**新しい診断システムを作る案件ではない。**
既に本番で何度も正常完了している診断処理へ、**10 人分をまとめた ZIP の各人の入力を
正しく渡す**案件である。

この前提と矛盾する設計になった時点で、実装せず理由を報告して STOP する。

### 0.1 V1 / V2 / V3 は全廃案

設計・実装・仕様書・テスト・validation・manifest・preflight・独自 mapping・
独自 state machine を**新設計の参考実装として使用しない**。
撤回の内訳は上記 2 コミットの本文にある。**旧仕様を正本にしない。**

---

## 1. 設計を支配する 2 つの事実

### 事実 1 — Production の診断処理は完成・実証済み

入力 → 既存解析 → 必要な LLM 処理 → Elith 仕様の JSON → S3 → Elith → Elith 側で正常処理完了、
までを何度も実データで完了している。

したがって **JSON 生成方法 / 検査項目の解釈 / LLM prompt / parser / canonicalization /
項目名変換 / Elith schema / S3 納品形式 / 検査別処理 を今回の案件用に再設計しない。**

### 事実 2 — 今回の入力も Production の入力要件を満たす

違いは **1 つの ZIP に 10 人分がまとまっている**ことだけ。
ZIP 内部の各人の実データ自体は、事実 1 の処理が受け付ける形をしている。

### 1.1 帰結

```
ZIP → 人物単位に分離 → 既存 Production 処理 → 既存 JSON → 確認 → 既存 S3 → Elith
```

**今回専用に新しい診断パイプラインを作らない。**

---

## 2. 実測した Production 処理マップ

すべて `design/transcos-zero-base` (撤回後) の実コードから。**関数名・ルートは推測していない。**

### 2.1 共通

| 役割 | ファイル | 関数 / 定数 |
|---|---|---|
| S3 設定 | `src/lib/s3.ts` | `getS3Config()` / `isS3Configured()` (`:63` / `:76`) |
| S3 書込 | `src/lib/s3.ts` | `putFiles()` (`:160`) |
| Bearer 認可 | `src/lib/api-auth.ts` | `isAdminAuthorized()` (`:61`) |

S3 キー (3 系統共通):
`{prefix}user/{client_id}/date/{YYYY_MM_DD}/{format_id}_date_{YYYY_MM_DD}_user_{client_id}.json`

**`AWS_REGION` 未設定 = 全系統ドライラン** (`s3.ts:63-65` が null を返し、各 API は
`configured:false` + `preview` を返して書かない)。**これが V4 の最重要の安全装置**。

### 2.2 入力別

| 入力 | 人数 | 既存入口 | 既存処理 | 入力の形 |
|---|---:|---|---|---|
| 健診 (複数枚) | 10 | `POST /api/admin/elith-hc-merge`<br>`action=part` → `finalize` | `src/lib/elith-export.ts` `scanImageToParsed()` (`:1308`) | part = 画像 data URL + `seq`<br>finalize = part 応答の `parts[]` |
| 健診 (1 枚) | — | `POST /api/admin/elith-scan` | `src/lib/elith-export.ts` `buildElithScanBundle()` (`:1392`) | data URL + `formatId` + `checkOnly` |
| 遺伝子 | 10 | `POST /api/admin/elith-genetic-merge`<br>`action=part` → `finalize` | `src/lib/elith-genetic.ts` `scanGeneticPage()` (`:158`) | **1 ページ = 1 画像 = 1 req** + `page` |
| 問診 | 10 | `POST /api/interview/export` | `src/lib/interview-export.ts` `buildElithInterviewBundle()` (`:274`) | **`answers: Record<string, string\|string[]\|number>`**<br>**LLM 不使用** |

### 2.3 既存の管理 UI と中継

UI は wellfort-site、処理は Scan-Chat-AI。中継はユーザートークン + `admin_users` 照会 →
上流へ `Bearer SCAN_CHAT_AI_API_KEY` の 2 層。

| 系統 | 管理 UI | 中継 (wellfort-site) |
|---|---|---|
| 健診・遺伝子 | `src/pages/admin/elith-batch.astro` | `src/pages/api/admin/elith-{scan,hc-merge,genetic-merge}.ts` |
| 問診 | **存在しない** | **存在しない** |

---

## 3. 設計の核心 — 既存 Production はすでにブラウザ駆動である

`elith-batch.astro` は **ファイルをブラウザで読み** (`:366-369` `FileReader.readAsDataURL`)、
**PDF をブラウザでページ画像へ展開し** (`:459` `ensurePdfjs` / `:493` `renderPdfPages`)、
**1 件ずつ既存 API を呼ぶ**。サーバはファイルを受け取らず data URL を受け取る。

→ **ZIP を解く場所もブラウザである。** これは好みではなく、既存入口の入力形が
data URL だからそうなる。この 1 点から以下が自動的に決まる:

- ZIP を S3 へアップロードしない → **presigned チケット不要・S3 の新しい置き場不要**
- サーバに ZIP を渡さない → **サーバ側 ZIP reader / Range GET / 分割読み 不要**
- 関数の実行時間を延ばす理由が無い → **`astro.config.mjs` の `maxDuration` を触らない**
  (V1/V2 は 60→800 秒へ上げていた。V4 では**戻したまま**)
- 新しい API を Scan-Chat-AI に作らない → **`src/pages/api/admin/` に新規ルート 0 本**

**V1/V2 が DB 6 テーブル・S3 アップロード口・専用 API 13 本・サーバ側 ZIP 基盤を
必要としたのは、ZIP をサーバへ持ち込む設計を先に選んだからである。**
その前提を外すと、それらは全部要らなくなる。

---

## 4. V4 の全体像 — 管理者ダッシュボード「臨時診断バッチ」

### 4.0 裁定 — **ZIP を選ぶだけ・ワンクリックで S3 書き出しまで** (2026-09-15 発注者)

> 「管理者ダッシュボードの『臨時診断バッチ』メニューの内容も、ZIP を選択するだけで、
> ワンクリックで S3 書出しまで進めて」

**画面の操作は 2 つだけ。** ①ZIP を選ぶ ②実行を押す。
10 名 × 3 検査 (健診 / 遺伝子 / 問診) を**止まらずに通し、S3 へ書き出すところまで**行く。

### 4.1 画面

```
[/admin/臨時診断バッチ]

 ① ZIP を選ぶ            [ファイル選択]  ← これだけ
 ② □ 最初の 1 名だけ流す (試運転)
 ③ [ 実行 ]  ← ワンクリック

 ─── 実行結果 ──────────────────────────────
  人物   検査      状態   test_date   納品キー
  01     健診      ok     2026-03-…   user/{client_id}/date/…/HealthCheckupData_….json
  01     遺伝子    ok     …           …
  01     問診      ok     …           …
  …
 [ 対応表 CSV をダウンロード ]   ← 人物フォルダ名 ↔ client_id (PII なので社内保管)
```

### 4.2 1 クリックの中で起きること

```
[管理者のブラウザ]
  ZIP を選ぶ
   ↓ JSZip 等で展開 (ブラウザ内・ZIP をサーバへ送らない)
  人物フォルダごとに分離
   ↓ 人物ごとに client_id を採番 (§8.4 案B = crypto.randomUUID())
   ↓ 人物内のファイルを検査種別へ分類 (実物の構造から決めた規則・不明なら止める)
  ┌────────────────────────────────────────┐
  │ 人物ループ (10) × 検査ループ (3)                   │
  │   健診 PDF  → renderPdfPages → hc-merge part×N     │
  │                              → hc-merge finalize   │→ S3
  │   遺伝子PDF → renderPdfPages(10,35) → part×26      │
  │                              → genetic finalize    │→ S3
  │   問診XLSX  → 変換表 → answers → resolvePath 確認  │
  │                              → interview/export    │→ S3
  └────────────────────────────────────────┘
   ↓
  結果表 + 対応表 CSV
```

**診断処理そのものは 1 行も書かない。** 書くのは**順番に呼ぶこと**と**見せること**だけ。
**唯一の例外が問診の変換表** (§5.2 裁定 P-3 / §5.3.1 完全オーダーメイド)。

### 4.3 ワンクリックが成立する条件 (3 つとも満たしている)

| # | 条件 | 満たし方 |
|---|---|---|
| 1 | **client_id を人に入力させない** | §8.4 **案B (uuid を採番)** なら ZIP だけで完結する。**案A (既存の diagnostic_user_id) を採ると 10 名分の事前入力が要るので、ワンクリックにならない** |
| 2 | **遺伝子のページ範囲を人に入力させない** | 既定 10〜35 で固定 (§9)。入力欄は残すが触らなくてよい |
| 3 | **問診を人に入力させない** | XLSX 8 名は変換表で自動 (§5)。**紙の PDF 2 名だけは手入力**。→ **10 名のうち 8 名がワンクリック、2 名は問診だけ手入力が残る** |

**3 は ZIP の中身がそうなっている以上、避けられない。** 紙の PDF にパーサを作らない
(§5.3 #9) 以上、2 名の問診は人が入れる。**それ以外は一度も止まらない。**

### 4.4 止まるのは 2 つの場合だけ (fail-closed)

1. **分類できないファイルがあった** — 人物フォルダの中に、健診/遺伝子/問診のどれとも
   決められないファイルがある。**推測で振り分けない。**
2. **問診の変換表に無い列・値があった** (§5.3 #5)。

どちらも**その人物だけ**を「要確認」にして、**残りの人物は最後まで通す。**
1 人で全体を止めない。

---

## 5. 問診 — 唯一「そのまま呼べない」入力

### 5.1 事実 (実測)

```
既存入口:  POST /api/interview/export   (src/pages/api/interview/export.ts)
受ける形:  answers オブジェクト (:16,38,68)。
           XLSX / PDF を読む処理は LLM 含め一切持たない
           (interview-export.ts の gemini 参照 0 件)。
ZIP:       Google Forms 由来 XLSX 8 名 / 紙の PDF 2 名
```

健診と遺伝子は入力が画像なのでそのまま渡せる。**問診だけが形が違う。**

**そのまま使える引数** (`export.ts:13-18`): `clientId` / `dateOfBirth` / `sex` /
`completedAt`。→ **client_id も test_date も呼び出し側から渡せる**ので、
この 3 点のために新設するものは無い。`AWS_REGION` 未設定ならドライラン (`:93-104`)。

**【重要】値の検証はどこにも無い。**

- `buildAnswers` は `QUESTIONS[id]` を**メタデータの参照にしか使わず**、値を選択肢と
  照合しない (`interview-export.ts:76-96`)。未知の id も `section_id: null` のまま通る。
- `formatAnswerLabel` は join / `String()` するだけ (`interview-script.ts:699-708`)。

→ **P-3 の正しさを保証するものは変換表だけで、後段に受け皿は無い。**

**【重要】分岐 (`when`) はこの経路では走らない。**

- `when` を評価するのは `InterviewEngine` / `resolvePath` だけ (`interview-script.ts:552`)。
- 条件はラベルの完全一致 (例 `:330` `a['S-STATUS'] === '過去に吸っていたが現在は吸わない'`)。

→ engine を通さない P-3 では、`S-STATUS`=吸ったことはない なのに `S-COUNT` に本数が入る、
といった **engine なら絶対に作らない組み合わせ**を誰も止めない。

**production の engine が実際に書く値の形** (ここに合わせる):

| 種別 | 例 | 値 | 根拠 |
|---|---|---|---|
| text (numeric) | `B-HEIGHT` | `"172"` — **単位を付けない** | `interview-script.ts:261-265` の `example: '172cm → 172'` |
| chip | `SL-QUALITY` | ラベル 1 個の文字列 | `interview-script.ts:497-500` |
| multi / list(multi) | `H-PAST` | ラベルの `string[]` | `formatAnswerLabel :700-703` |
| slider | `SL-STRESS` | `"7 / 10"` — **数値ではなく文字列** | `live-controller.ts:431` |
| matrix | `F-FREQ` | `"野菜・海藻類：ほぼ毎日 / フルーツ：週2〜3回 / …"` | `choice-picker.ts:630-634` |

### 5.2 裁定 — **P-3** (2026-09-15 発注者)

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| P-1 | 本人に問診アプリを通してもらう | 新規実装ゼロ。production の分岐・検証がそのまま効く | 10 名に操作を依頼する必要がある |
| P-2 | 管理者が問診アプリの画面に代理入力する | 新規実装ゼロ。answers は engine が作るので正しさが担保される | 10 名 × 約 35 問の手入力 |
| **P-3** ← **採用** | XLSX の列 → `question_id` の**変換表**を書く | 8 名ぶんが自動。PDF 2 名は結局手入力 | 今回専用の変換規則が生まれる。正しさの保証が engine から変換表へ移る |

当初の推奨は P-1 / P-2 だった (理由 = 5.1 のとおり engine が保証していたものが
変換表の正しさに置き換わるため)。**発注者裁定により P-3 で進める。**
保証が移る以上、**移った先を落とさないための枠が 5.3**。

### 5.3 P-3 の枠 — 「今回限りの入力アダプタ」

**位置づけ: 診断パイプラインの一部ではない。** 今回 10 名を通すための入力アダプタであり、
案件終了後に削除しても production の問診が壊れないこと、を設計の条件とする。

| # | 守ること | 理由 |
|---|---|---|
| 1 | **別ファイルに閉じる**。production の `src/lib/**` / `src/pages/api/**` から import されない | 案件終了後に消せる状態を保つ |
| 2 | **語彙は production の定義をそのまま読む** — `QUESTIONS[id]` の `chips` / `multi_options` / `list_options` / `matrix_rows` / `matrix_cols` を許可値の唯一の出典にする。**選択肢の一覧をアダプタ側に書き写さない** | §12 #7「独自 canonical table を作らない」。写すと production 側の選択肢変更と黙って食い違う |
| 3 | **値の形は 5.1 の表に合わせる**。matrix は `formatMatrix()` を、ラベル整形は production の関数を **import して使う**。再実装しない | §12 #5「Production と同じ処理をコピーする」 |
| 4 | **分岐の整合は production の `resolvePath(answers)` で確認する** (`interview-script.ts:549` から export 済み)。生成した id 集合が `resolvePath` の結果と一致しなければ**そこで止める** | engine が走らないぶんの穴を、engine 自身の関数で塞ぐ。整合ロジックを自作しない |
| 5 | **fail-closed**。未知の列 / 許可値に一致しない値 / 表現できない回答は、**推測で寄せず・黙って捨てず**、その人物を「要確認」にして人へ出す | 5.1 のとおり後段に受け皿が無い。捏造ゼロ |
| 6 | **LLM・fuzzy・部分一致・意味推定を使わない**。正規化は NFKC + trim + 連続空白の縮約まで。**それでも足りない差が実在する (§15.3 波ダッシュ)** — 正規化を増やさず、**実物の文字をそのまま表のキーにする** | 決定論。問診は元々 LLM を使わない処理 |
| 7 | **変換表は実物の XLSX から起こす**。列見出しと選択肢の**実際の文字列を verbatim で**表に書き、出典 (ファイル名・列位置) を併記する | CLAUDE.md R2「存在するかを先に確認する」。**本作業環境に ZIP が無いので、表は実装ステップ 1 で実物を開いて書く。いま推測で書かない** |
| 8 | **複数回答を 1 件に落とさない / 表現できない値を切り捨てない**。Google Forms の複数選択は区切り文字で 1 セルに入るので、**区切りも実物で確認してから**決める | 情報の欠落は画面上は正常に見える |
| 9 | **PDF 2 名にパーサを作らない**。P-1 / P-2 と同じ手入力で通す | §12 #6「独自 parser を先に作る」 |
| 10 | 変換表と、**人が目で確認するための「XLSX の生の値 → 送る値」の対照表示**を画面に出す | 納品前に人が確認できることが、変換表の唯一の検算手段 |

**表に無い列は「未対応」として人へ出す。**「安全のため」に既定値を入れない (§12 #10)。

### 5.3.1 裁定 — **完全オーダーメイド。他案件で使い回さない** (2026-09-15 発注者)

> 「既に入力データ (ZIP) が確定して実データもあるので、内容を確認して、
> 完全オーダーメイド (他の案件で使い回しはしない)」

**これは 5.1 の 3 つの懸念に対する答えになっている。** 入力が**閉じた有限集合**
(8 ファイル・列数も選択肢も確定済み) になるので、**一般的な検証を作る代わりに、
実物を網羅で潰せる**。

| 5.1 の懸念 | 一般実装なら | 完全オーダーメイドなら |
|---|---|---|
| ① 値の検証が後段に無い | 汎用 validator を書くことになる (§12 #8 に触れる) | **8 ファイルの全セルが「写像済み」か「未対応として人へ」のどちらかであることを 1 回数え切る**。網羅が検証の代わりになる |
| ② `when` が走らない | 整合ロジックを自作することになる | **8 名ぶんを `resolvePath` に通して一致を確認するだけ**。仕組みでなく実測 |
| ③ 値の形が素直な型と違う | 全設問ぶんの汎用変換が要る | **実際に出現する設問ぶんだけ**、5.1 の表に合わせて固定する |

**したがって守ることが 5.3 に 4 つ足される。**

| # | 守ること | 理由 |
|---|---|---|
| 11 | **一般化しない。** 列の判定は**実際の見出し文字列の完全一致**のみ。パターン照合・正規表現での吸い込み・「Google Forms 一般」への対応をしない | 一般化した瞬間に、確定しているはずの入力に対して未知の分岐が生まれる |
| 12 | **網羅を数える。** 8 ファイルの列を全部列挙し、各列が「どの `question_id` へ写るか」「写さない (理由つき)」のどちらかに決まっていることを**件数で示す**。取りこぼし 0 を数字で確認する | ①の代わり。目視では抜ける |
| 13 | **8 名ぶんの結果を人が 1 回通して見る。** `resolvePath` 一致の確認と、XLSX の生の値 → 送る値の対照表示 (§5.3 #10) を 10 名ぶん出し切る | ②の代わり。10 名しかいないので全件確認できる |
| 14 | **案件名を冠した 1 ファイルに閉じ、他から import しない。** 汎用の名前を付けない (`xlsx-to-answers` のような名前にしない) | 「使い回さない」を名前とファイル境界で担保する。次の案件で流用されると、確定していない入力に確定前提の表が当たる |

**本作業環境に ZIP が無い。** 「内容を確認して」に当たる作業 (列見出しと選択肢の採取) は
**実物を受け取ってから**行う (§5.3 #7 / §14 step 2)。**いま推測で表を書かない。**

### 5.4 中継が 1 本だけ要る — 理由は認可ではなく CORS

`/api/interview/export` には **Bearer 認可が無い** (`export.ts` の import に `api-auth` が無い
= エンドユーザー経路として設計されている)。一方 Scan-Chat-AI 側に CORS ヘッダを付ける
middleware は無い (`src/middleware*` 無し / `access-control-allow-origin` の実装 0 件)。
→ wellfort-site の管理画面 (別オリジン) のブラウザから直接は呼べない。

**既存の中継 (`elith-genetic-merge.ts` 86 行) と同型を 1 本足す。**
入口の admin 判定 (ユーザートークン + anon apikey → `admin_users`) はそのまま流用する。
**この中継が新たに開けるのは「管理者が任意の client_id で問診 JSON を書ける」だけ**で、
これは既存の `elith-scan` / `elith-hc-merge` で管理者が既に持っている権限と同じ。

---

## 6. S3 に何が書かれるか / やり直し方

### 6.1 実測 — 書き込みは 4 か所

| 経路 | 何を書くか | 根拠 |
|---|---|---|
| `elith-hc-merge` `action=part` | **1 枚ごとに元画像を即 PUT** (`checkOnly` 相当の抑止は無い) | `:117-121` |
| `elith-hc-merge` `action=finalize` | 納品 JSON **1 個だけ** (manifest は書かない) | `:262` |
| `elith-genetic-merge` `part` | **書かない** | ファイル冒頭に明記 |
| `elith-genetic-merge` `finalize` / `interview/export` | 納品 JSON | `:184` / `export.ts:107` |

### 6.2 裁定の更新 — **ワンクリックで S3 まで通す** (2026-09-15 発注者・§4.0)

§4.0 の裁定により、**Q1 の「ドライランで解析を回す」は今回は採らない。**

**理由は技術的に両立しないから。** ドライランは `AWS_REGION` という**サーバ全体の env**
で決まる (`s3.ts:63-65` — 「AWS_REGION の有無を書き出し有効化のスイッチとする」) ので、**1 回の実行の中で「解析はドライラン・納品は本番」に
切り替えられない**。切り替えるには 2 回に分けて間でデプロイが要る。

**帰結 (承知のうえで進める)**:

- **健診の元画像が S3 に残る** (`elith-hc-merge:117-121`)。これは**通常運用と同じ挙動**で、
  監査のための既存仕様。Q1 の当初案 (残さない) とは逆になる。
- **`elith-hc-merge` の `part` は改造しない** (通常運用の健診バッチを変えない)。

### 6.3 それでも危険が小さい根拠 (実測)

1. **S3 に書いても Elith は動かない。** Elith への**起動トリガ (通知手段) は未確定**
   (`elith_s3_data_handoff_spec.md §8.2` 「フォルダ完成を Elith に通知する手段」= **要確認**)。
   読み取りが Pull 型かどうかも要確認。**書いた事実が自動で何かを起こす経路は無い。**
   Elith に渡すのは Wellfort が別途知らせたときだけ。
2. **やり直せる。** 同一 `(client_id, date, format_id)` は**同一キーで上書き**される
   (同 §8.2 冪等性 / キーは `elith-export.ts:1459-1460` の決定論)。
   同じ ZIP をもう一度流しても**ファイルは増えない** (Test F)。
3. **最初の 1 名だけ流せる** (§4.1 ②)。10 名を一気に通す前に 1 名で結果を見られる。

### 6.4 確認は「書いたものを見る」で行う

承認ゲートを前に置かない代わりに、**実行結果の表に納品先のフルパスをそのまま出す**
(§8.5 #4)。`user/{client_id}/date/{YYYY_MM_DD}/…` を人が読んで確認できることが最後の砦。

**間違いに気づいたときの直し方**: 入力を直して**同じ client_id で流し直す** (上書き)。
**client_id 自体を間違えたときだけ**、誤ったキーの削除が要る
(本番の納品と同じ名前空間にあるので、削除は個別に特定してから — 撤回報告の Q6 と同じ注意)。

---

## 7. 進捗の持ち方

### 7.1 規模

10 名 × (健診 1〜数枚 + 遺伝子 26 ページ) ≒ **270 回以上の LLM 呼び出し**。
全部やり直すとコストと時間が無視できない。

### 7.2 方針 — DB を作らない

**新しいテーブルを作らない** (V1/V2 の 6 テーブル + `executive_subjects` は撤回済み)。
part の応答をブラウザ側 (IndexedDB) に (人物, ファイル, ページ) をキーに保持し、
**再開時は保持済みを飛ばす**。

- ブラウザを閉じても残る (localStorage / IndexedDB はタブを越えて残る)
- **PII を持つ**ので、納品完了後に**明示的に消す導線を置く**
- 別の PC では再開できない (今回 1 人の管理者が通す前提なので許容)

> **裁定 Q2 (2026-09-15 発注者) = 了承。**
> 1 台のブラウザで通す。別 PC では再開できない。DB は作らない。
> **帰結として守ること**: ①作業中にそのブラウザのサイトデータを消さない
> ②納品完了後に**明示的に消す導線**を必ず用意する (PII がブラウザに残るため)
> ③プライベートウィンドウで作業しない (閉じると消える)。

---

## 8. Executive (client_id) の決め方 — Q3 の提案

### 8.1 実測した事実

**仕様上の定義** (`docs/elith/elith_s3_data_handoff_spec.md:44` / §2.1):

- `client_id` = **顧客を一意に識別する ID**。Wellfort の `diagnostic_user_id` (uuid) を充てる。
- 理由は **PII を含まない橋渡しキーだから**。S3 のパスとファイル名は Elith と共有されるので、
  **ここに PII を絶対に載せない**。
- `client_id` は**通年で不変**。回の分離は日付フォルダで行う (`:105`)。
- なお **Elith への確認 #1「client_id に uuid を用いる前提でよいか」は未回答のまま** (`:520`)。

**コードは client_id を検証しない** (実測):

| 経路 | 未指定のとき | 根拠 |
|---|---|---|
| `elith-hc-merge` | **400 で止まる** | `:92-93` |
| `elith-genetic-merge` | **400 で止まる** | `:85-86` |
| `elith-scan` | **黙って `randomUuid()` で納品する** | `:93` |
| `interview/export` | **黙って `diagnosticUserId → diagnosticId`(新規 uuid) で納品する** | `interview-export.ts:252` / `export.ts:73` |

- **形式の検査は 1 か所も無い。** 値はそのまま S3 キーへ文字列連結される
  (`elith-export.ts:1459-1460`)。DB 照会も無い (3 経路とも supabase 参照 0 件)。
- → **存在しない ID でも納品は成功する。** 止めるのは V4 の画面の役目 (§8.5)。

**production の前例 = uuid でない ID も既に納品されている**:

- 既存 admin の「② client_id の付け方」は **自動採番 (`test-…`) / 固定 ID** の 2 択
  (`wellfort-site src/pages/admin/elith-batch.astro:69-75`)。自動は `test-{stamp}-001` (`:1434`)。
- 仕様書のサンプルにも `"client_id": "elith-test-003"` がある (`:349`)。

**uuid でないと後から本人に繋げられない** (決定的):

- `diagnosis.app_users.diagnostic_user_id` は **`uuid` primary key** (`supabase/migrations/20260601000010_schemas_and_tables.sql:174`)。
  `customer.customer_profiles.diagnostic_user_id` も `uuid unique` (`:53`)。
- Elith から返る報告書の置き場 `diagnosis.diagnosis_results` は
  `app_users(diagnostic_user_id)` への FK (`:231`)。
- → **`transcos-01` のような文字列は、後から app_users に載せられない。**

### 8.2 分岐点 — 「この 10 名はアプリで結果を見るか」

**ここだけが案を分ける。** 他は全部同じ。

- **見せない** (Elith の診断結果は Wellfort が受け取り、アプリの外で 10 名へ渡す)
  → client_id は納品の宛名にすぎない。何でもよい。
- **見せる (将来を含む)**
  → client_id は `app_users.diagnostic_user_id` **でなければならない** (8.1 の FK)。
  後から変えるには**納品をやり直す**しかない (S3 のパスにもファイル名にも入っているため)。

### 8.3 案の比較

| | 案A 既存の診断ユーザーを使う | **案B 今回用に uuid を採番** | 案C 読みやすい ID を振る |
|---|---|---|---|
| 例 | `customer_profiles.diagnostic_user_id` | `3f9c…` (uuid v4) | `transcos-01` |
| 前提 | **10 名が EC の顧客であること** | なし | なし |
| 仕様 §2.1 との整合 | ○ | ○ | △ (uuid でない) |
| PII | ○ 含まない | ○ 含まない | **△ 取引先名が Elith と共有するパスに載る** |
| 後から本人に繋げる | ○ そのまま | ○ **そのまま app_users に載せられる** | **× uuid 列に入らない＝納品やり直し** |
| 照合の手間 | **氏名で当てられない** (CLAUDE.md「氏名 OCR のみでの顧客割当確定は禁止」) → 1 名ずつ人が確認 | 対応表を作るだけ | 対応表を作るだけ |
| S3 の後片付け | 本番の顧客と同じ名前空間に混ざる | 同上 (対応表が要る) | ○ 前置詞で一括特定できる |

### 8.4 提案 — **案B (今回用に uuid を採番)**

理由は 3 つ。

1. **10 名が EC の顧客だとは限らない。** 案A はその前提が崩れると使えない。
   production には既に前例がある — 顧客でない人 (記者・パートナー) に
   `crypto.randomUUID()` を振る経路がある (`src/lib/demo-accounts.ts:291`)。
2. **案C は後戻りできない。** 8.1 のとおり `app_users` は uuid 列なので、
   `transcos-01` で納品した後に「やはりアプリで見せたい」となったら**納品のやり直し**になる。
   uuid なら**そのまま** `app_users` に載せられる。「読みやすさ」は§8.5 の対応表と画面のラベルで
   足りるので、**後戻りできない代償を払う理由が無い。**
3. **案C は取引先名を Elith と共有するパスに書く。** 既存ポリシーが禁じているのは個人の PII だが、
   `transcos-01` + 納品日は文脈次第で個人に辿れる。**uuid にすれば論点自体が消える。**
4. **案B でないとワンクリックにならない** (§4.0 の裁定)。案A は 10 名分の
   `diagnostic_user_id` を人が事前に入れる必要があり、「ZIP を選ぶだけ」が崩れる。
   案B なら**採番は実行の中で行える**ので、画面の入力は ZIP 1 つで済む。

**採番のしかた**:

- **`crypto.randomUUID()` を 10 個**。人物フォルダとの対応は**管理者が画面で 1 名ずつ確認して確定**する
  (V4 は氏名から自動で当てない)。
- **対応表 (氏名 ↔ uuid) は PII なので Elith へ渡す物には入れず、Wellfort 側だけで保管する。**
  納品物・S3 のパス・JSON 本文のどこにも氏名を入れない (既存仕様どおり)。
- **一度決めたら変えない** (`client_id` は通年不変・`:105`)。

**アプリで結果を見せるなら、追加で 1 手**: 採番した uuid で `diagnosis.app_users` に行を作る。
**これは migration ではなくデータ投入**だが、**production DB への書き込み**なので**別途承認が要る**。
V4 の納品自体はこの行が無くても成立する (8.1 のとおり DB を見ないため)。

### 8.5 案がどれでも守ること — **client_id を省略できない画面にする**

8.1 の実測のとおり、**4 経路のうち 2 経路は client_id が無くても黙って別の ID で納品する。**
これは「間違いに気づけない失敗」なので、V4 側で構造的に塞ぐ。

1. **10 名全員の client_id が確定するまで、納品ボタンを押せない。**
2. **呼び出しの直前に、その人物の client_id が入っていることを必ず確かめる。**
   `elith-scan` / `interview/export` の既定値に**一度も頼らない**。
3. **人物 × 検査の全呼び出しで同じ client_id を使う。** 健診・遺伝子・問診が別 ID になると、
   Elith 側では**別人 3 人**に見える (フォルダが `user/{client_id}/` で分かれるため)。
4. **Human Review 画面に、納品先のフルパスをそのまま出す。**
   `user/{client_id}/date/{YYYY_MM_DD}/…` を人が読んで確認できることが最後の砦。

### 8.6 発注者に決めてほしいこと

| | 内容 |
|---|---|
| **① 必須** | この 10 名は**アプリで結果を見るか** (§8.2)。見ないなら案B のまま進められる |
| **② 必須** | 案B (uuid 採番) でよいか。**案A を採るなら、10 名が EC 顧客である確認と、誰がどの `diagnostic_user_id` かの提示が要る** (氏名から当てない)。**加えて案A はワンクリックを壊す** (§4.3 #1) |
| ③ ①が「見る」の場合 | 採番した uuid で `diagnosis.app_users` に行を作ってよいか (production DB への書き込み) |

---

## 9. 遺伝子のページ範囲

Genoplan は 208 または 210 ページあるが、**疾患リスク倍率を持つ項目が印字されているのは
p10〜35 の 26 ページだけ**。根拠 = `docs/scan/golden/scan_golden_genetic_geneplanet_20240131.md`
(この範囲から 220 項目を建立)。

**既存 UI が編集可能な from/to を持っている**ので、V4 は**同じ入力欄を使う**。
**定数を新設しない** (V1/V2 が `GENOPLAN_V1_REQUIRED_PAGES` を作ったが撤回済み)。
既定値に 10 / 35 を入れておき、**残りのページを LLM へ送らない**ことだけ守る。

---

## 10. テスト設計

**大量の unit test から始めない。** 順に通す。
**A / B は §4.1 の「最初の 1 名だけ流す」チェックでそのまま実行できる** (テスト専用の導線を作らない)。

| # | 内容 | 合格条件 |
|---|---|---|
| **A** | 実 ZIP から 1 人を取り出す | 人物フォルダと所属ファイルが画面に正しく出る |
| **B** | その 1 人を既存 Production 処理へ流す | part / finalize が 200 を返し JSON が出る |
| **C** | **通常経路と同一であること** | **同じ builder を 2 つ比較するのではなく、`elith-batch.astro` と V4 が同じ API・同じ関数を呼んでいることをコードで示す**。加えて同一入力で JSON をバイト比較 |
| **D** | 10 人全員を実行 | 30 JSON (健診 10 / 問診 10 / 遺伝子 10) |
| **E** | **人物 A の入力が人物 B へ混ざらない** | 全 JSON の `client_id` と中身が人物と一致 |
| **F** | **同じ ZIP を 2 回流してもファイルが増えない** (冪等・§6.3 #2) | 2 回目の後も納品キーの数が 1 回目と同じ。内容も同じ |
| **G** | 既存 S3 納品で Elith へ渡り、Elith 側で正常処理できること | Elith からの完了確認 |

**Test C が最重要。** 「同じ処理を呼んでいる」ことを保証する形にする
(例: V4 のコードに `elith-export` / `elith-genetic` / `interview-export` の関数を
再実装した箇所が 0 件であることを機械で見る)。

---

## 11. 新規実装

### 11.1 作るもの

| # | 新設 | 置き場所 | 概算 |
|---|---|---|---|
| 1 | ZIP 展開 + 人物分離 | wellfort-site `src/scripts/` | 1 ファイル |
| 2 | 呼び出し順の制御 (人物 × 検査 × ページ) | 同上 | 1 ファイル |
| 3 | 進捗保持と再開 (IndexedDB) | 同上 | 1 ファイル |
| 4 | 画面「臨時診断バッチ」(ZIP 選択 / 実行 / 結果表 / 対応表 CSV) | wellfort-site `src/pages/admin/` | 1 ページ |
| 5 | 問診アダプタ (XLSX → answers) — **P-3 裁定済み** | wellfort-site `src/scripts/` (**production から import されない別ファイル**) | 1 ファイル |
| 6 | 変換表 (実物の列見出し → `question_id` / 値 → 許可ラベル) | 同上 (5 と分けて data として持つ) | 1 ファイル |
| 7 | `/api/interview/export` への中継 | wellfort-site `src/pages/api/admin/` | 1 ファイル (既存 86 行と同型) |

**合計 7 ファイル + 1 ページ。** うち 5〜7 は **P-3 を採ったぶん**で、
案件終了後に削除しても production の問診は壊れない (§5.3 #1)。

### 11.2 作らないもの (既存呼び出しで済む)

健診の解析・JSON 生成 / 遺伝子の解析・JSON 生成 / 問診の JSON 生成 /
S3 納品 / Elith スキーマ / PDF → ページ画像 / 認可の 2 層 / 中継 (健診・遺伝子)。

### 11.3 触らないもの

- **Scan-Chat-AI の `src/lib/**` と `src/pages/api/**`** (新規ルート 0 本・既存改変 0 行)
- **`astro.config.mjs`** (`maxDuration` は案件前の 60 のまま)
- **DB** (migration 0 件)
- **決済 / EC**: `src/components/products/WellfortProductDetail.astro` /
  `src/pages/products/[id].astro` / `src/pages/api/payment-status*` / `src/lib/payment*` /
  `supabase/**` / checkout / GMO / Amazon Pay

---

## 12. 禁止事項

1. 旧 V1/V2/V3 コードを参考に新設計する
2. 旧仕様を正本にする
3. 新しい診断 pipeline を作る
4. 新しい JSON schema を作る
5. Production と同じ処理をコピーする
6. 独自 parser を先に作る
7. 独自 canonical table を作る
   — **P-3 の変換表はこれに当たらない条件で作る**: 許可値の出典は production の
   `QUESTIONS[id]` であり、アダプタ側に選択肢を書き写さない (§5.3 #2)。
   書き写した時点で #7 違反になる。
8. 大量の validation を先に作る
9. 大量の合成 fixture テストから始める
10. **「安全のため」という理由だけで処理を追加する**
11. 推測で Production 挙動を再現する
12. **共有 core 化を「綺麗になるから」で行う** — まず既存 API をそのまま呼ぶ方法を検討し、
    どうしても不可能な場合だけ最小限の切り出しを**案として提示**する (承認前に実装しない)

---

## 13. 発注者確認事項

### 13.1 裁定済み (2026-09-15)

| # | 内容 | 裁定 | 章 |
|---|---|---|---|
| **Q1** | 解析フェーズをドライランで回すか | **一度は「ドライラン可」。その後 §4.0 の「ワンクリックで S3 まで」が出たため、今回はドライランを採らない** (両立しないため・理由は §6.2)。帰結として健診の元画像は通常運用どおり S3 に残る | §6.2 |
| **Q2** | 進捗をブラウザだけに持つ割り切りでよいか | **了承**。1 台のブラウザ・DB を作らない | §7.2 |
| **Q4** | 問診を P-1 / P-2 / P-3 のどれで通すか | **P-3**。枠は §5.3。さらに**完全オーダーメイド・他案件で使い回さない** (§5.3.1) | §5.2 |
| **Q3** | 10 名の `client_id` | **案B = 今回用に uuid を採番**。実行の中で採番するので画面の入力は ZIP 1 つで済む | §8.4 |
| **Q8** | `AJ` カフェイン量 / `AK` ご飯の量 | **そのままの内容で JSON に入れる** | §16.5 |
| **Q9** | 選択肢マスタに無い語 | **そのまま送る** (`その他` へ寄せない)。実測 12 語 | §16.4 |
| **Q10** | `BA`〜`BI` (がんリスク検査・AI疾病予測の設問) | **入れない** | §16.6 |
| **Q7** | 画面の操作 | **ZIP を選ぶだけ・ワンクリックで S3 書き出しまで** (§4.0) | §4 |

### 13.2 未回答 — **Q3 は納品を止める**

| # | 内容 | 影響 | 章 |
|---|---|---|---|
| **Q5** | 撤回した DB (6 テーブル + `executive_subjects`) を実際に DROP するか。`executive_subjects` は email の unique index を持ち **PII を含む** | 実装は進められる (V4 は DB を使わない)。**PII が残り続けるので別途判断が要る** | 撤回報告 |
| **Q6** | S3 の後片付け。`ad-hoc-uploads/` は案件専用で安全。**E2E が書いた納品 JSON 1 件は本番の納品と同じ名前空間**にあるので個別に特定が要る | 実装は進められる。**削除候補の提示のみで実削除はしていない** | 撤回報告 |

**Q3 は解析・確認までは進められるが、納品 (finalize + S3 write) に入れない。**
Q1 の裁定でドライランを回すので、**解析フェーズは Q3 の回答を待たずに始められる**。

---

## 14. 実装順序

**現在地: Q1〜Q4 / Q7〜Q10 裁定済み。変換表は §16 に確定 (実装待ち)。**
**残るのは Q5 / Q6 (撤回した DB と S3 の後片付け) だけで、どちらも実装を止めない。**
**実物の ZIP を受領し数え終えた (§15)。step 2 の材料は揃っている。**

| # | 内容 | 前提 |
|---|---|---|
| 0 | ~~**ZIP の中身を数える**~~ **完了 (§15)** — 人物フォルダの構成 / ファイル名の規則 / どのファイルがどの検査か / 問診 XLSX の**列見出しと選択肢**。**画面の「ZIP を選んで数えるだけ」で出す**ので、150MB の ZIP も PII もこの場に渡さずに済む (§14.1) | ZIP (管理者の手元) |
| 1 | 「臨時診断バッチ」画面 — ZIP 選択 + 最初の 1 名だけ (= Test A / B) | 承認 |
| 2 | ~~変換表を起こす~~ **完了 (§16)**。実装はこの表を別ファイルへ写すだけ | — |
| 3 | 問診アダプタ + 中継。`resolvePath` 一致と対照表示 (§5.3 #4 #10 / #12 #13) | 2 |
| 4 | Test C (通常経路と同一であることの証明) | 1 |
| 5 | 10 名を通す (Test D / E) + 結果表 + 対応表 CSV | 3, 4 |
| 6 | Test F (2 回流しても増えない) | 5 |
| 7 | Elith へ渡す → Test G | **Q3** |

**1 以降は発注者承認の後。** 本仕様書の時点では実装を開始していない。

### 14.1 step 0 — ZIP をこの場に渡さずに中身を数える

**ZIP は 150MB あり、10 名分の検査結果 (PII) を含む。**
クラウドの作業環境へ持ち込まずに済ませる。

- 本設計はもともと**ブラウザ駆動**で、**ZIP をサーバへ送らない** (§3 / §4.2)。
  したがって**開発にも実物のバイト列は要らない**。要るのは**構造と文字列**だけ。
- step 1 の画面の最初の機能として**「ZIP を選んで中身を数えるだけ」**を作り、
  管理者のブラウザで実行して**その出力 (下記) だけ**を共有してもらう。

**出力するもの (PII を含まない)**:

1. 人物フォルダの数と、**フォルダ名の形** (氏名そのものは伏せ、`<氏名>` と置く)
2. 人物あたりのファイル名の一覧 (**氏名部分は同じく伏せる**)・拡張子・ページ数
3. 問診 XLSX の**列見出しの全文** (行データは出さない)
4. **選択肢の列だけ**、出現した値の一覧 (自由記述の列は「自由記述」とだけ出す)

**出さないもの**: 検査値・氏名・生年月日・住所・自由記述の中身。

---

## 15. 実物 ZIP の実測 (2026-09-15)

**発注者から実物 (159,308,299 バイト) を受領し、全ファイルを数えた。**
**本節に個人名・生年月日・検査値・自由記述の中身は書かない。**
書くのは**構造と語彙**だけ (変換表を書くのに要るものだけ)。

### 15.1 構成

```
20260910　【トランスコス10名分】の血液・健康データ/     ← ルート 1 つ
  10名の情報.xlsx          No. / 氏名 / 実施日 の 3 列・10 行
  ~$10名の情報.xlsx        Excel のロックファイル (165 B・中身なし)
  問診サイト.docx          Microsoft Forms の URL と OneDrive リンクだけ
  １．<氏名>フォルダー/ … １０．<氏名>フォルダー/   ← 人物フォルダ 10
```

**38 ファイル。** 人物フォルダの中身は次の 6 種類。

| 種別 | ファイル | 人数 | 実測した形 |
|---|---|---|---|
| **遺伝子** | `CFBB-…` / `CGAC-… (<氏名>1).pdf` 約 21 MB | **10 / 10** | **208 ページ 8 名 / 210 ページ 2 名** |
| **健診 A** | `<氏名>.pdf` 約 106 KB | 5 | 1 ページ・A4 横・**3512×2480 px・CCITT 1bit のスキャン画像** |
| **健診 B** | `{社員番号}_<姓>_<名>.pdf` 約 6 KB | 5 | 1 ページ・A4 縦・**テキスト PDF**「健康診断結果レポート」**28 項目すべて記載** |
| 健診 B 付属 | `その他（<氏名>）.xlsx` 約 11 KB | 5 | **健診 B と同一内容の構造化データ・39 列** |
| **問診 XLSX** | `ウェルテクト…（N．<氏名>）：共通問診表…(1-8).xlsx` 約 16 KB | **8** | **62 列 × 1 行**・8 本ともヘッダ完全一致 |
| **問診 PDF** | 2 名分 (5 ページ / 10 ページ) | **2** | どちらも A4 縦・**テキスト PDF** (画像 0) |

### 15.2 設計に効く実測 — **§4 の 1 クリックは変えなくてよい**

| # | 実測 | 設計への影響 |
|---|---|---|
| **F1** | 遺伝子が 208 / 210 ページ | CLAUDE.md の記述と一致。**§9 の p10〜35 固定がそのまま使える** |
| **F2** | 健診が 2 種類あるが**どちらも 1 ページの PDF** | **`renderPdfPages` → `elith-hc-merge` の既存経路をそのまま通せる。設計変更なし。** テキスト PDF (健診 B) はレンダリングすると**スキャンより綺麗な画像**になるので、むしろ読みやすい |
| **F3** | 健診 B の 5 名に**同内容の構造化 XLSX** がある | **納品経路にしない。** 5 / 10 名について「LLM が正しく読んだか」を決定論で照合できる = **🎯 の代わりになる**。§10 Test C の補強に使える |
| **F4** | 問診は **Microsoft Forms** 由来 (`問診サイト.docx`) | §5 の「Google Forms 由来」は**誤り**。訂正済み |
| **F10** | `10名の情報.xlsx` の**氏名の列は空で、名前は貼り付けられた画像** (`xl/media/image1.png`) | この表からは No. と実施日しか機械で読めない。**ただし `test_date` は production が画像から読む** (`elith-hc-merge` の応答 `test_date` / `date_source`) ので、**この表に依存しなくてよい** |
| F11 | 問診 XLSX の番号 (1〜8) と**フォルダ番号 (1〜10) が一致しない** | 人物の対応は**番号でなく氏名列で取る**。番号で突き合わせない |

### 15.3 【最重要】波ダッシュの文字コードが違う — 気づかないと静かに壊れる

**XLSX は `～` (U+FF5E FULLWIDTH TILDE) / production は `〜` (U+301C WAVE DASH)。**

**NFKC 正規化では一致しない** (実測: `NFKC(U+FF5E)` → `~` / `NFKC(U+301C)` → `U+301C` のまま)。
§5.3 #6 で「正規化は NFKC + trim + 空白縮約まで」と決めたが、**それだけでは足りない。**

- これだけで**選択式 11 種 + `F-FREQ` の列 2 種 (9 列に出現) = 13 行**が不一致になる (実測)。全数は §16.3。
- 影響する設問: `D-FREQ` / `D-AMOUNT` / `E-FREQ` / `E-TIME` / `M-PERIOD` / `SL-HOURS` /
  `F-FREQ`(マトリクス列 `週2〜3回` `週4〜5回`)。
- **失敗の仕方が悪い**: 例外にならず、ただ「変換表に無い値」として落ちる。
  fail-closed にしていれば要確認に出るが、**8 名ほぼ全員が要確認になって使い物にならない。**

**対処 = 変換表を実物の文字列 (U+FF5E) で書く。** §5.3 #7「実物から verbatim で起こす」を
守れば自動的に解決する。**production 側の文字を変えない。**
正規化に「U+FF5E → U+301C」を足す案は採らない — **見えない文字の置換規則が増えると、
次に同種の差 (ハイフン・中点・全角空白) が出たときに際限がなくなる**。
実物の文字をそのまま表のキーにするほうが、目で確認できる。

### 15.4 問診 62 列の内訳 (P-3 の作業量)

| 分類 | 列数 | 中身 |
|---|---|---|
| **そのまま一致** | 14 | 値が production の選択肢と**完全一致** (`L` `M` `N` `O` `P` `R` `Z` `AL` `AM` `AQ` `AT` `AY` ほか) |
| **波ダッシュ差だけ** | 6 + マトリクス 9 | §15.3。**実物の文字で表を書けば一致** |
| **型が違う (数値 → 区間)** | 4 | `S` 喫煙を止めた年齢 / `T` 本数 / `U` 年数 / `X` 飲酒年数。production は区間の chip。**決定論で落とせる** |
| **型が違う (自由記述 → 選択肢)** | 3 | `AR` 座位時間 (`8` と `8時間以上` と全角 `８` が混在) / `AJ` カフェイン / `AK` ご飯の量 |
| **選択肢マスタに無い語** | 9 語 | `H-CURRENT` に 1 / `H-PAST` に 4 / `H-SYMPTOMS` に 1 / `E-TYPE` に 2 / `F-DIET-METHOD` に 4 (「なし」系の表記ゆれ 3 を含む) |
| **production に無い設問** | 11 | `G` `H` `I` (氏名/生年月日/性別) + `BA`〜`BH` (がんリスク検査) + `BI` (AI疾病予測) + `BJ` (同意) |
| 事務用 | 6 | `A` ID / `B` 開始時刻 / `C` 完了時刻 / `D` メール(全員 `anonymous`) / `E` 名前(空) / `F` 最終変更時刻(空) |

**判断が要るのは 3 種類だけ** (§15.5)。残りは機械で決まる。

### 15.5 発注者に決めてほしいこと (問診)

| # | 内容 | なぜ決められないか |
|---|---|---|
| **Q8** | `AJ` カフェイン量 / `AK` ご飯の量 を**どう扱うか** | 自由記述で、production の選択肢へ**決定論で落とせない**。①要確認にして人が選ぶ ②その設問を送らない (未回答) のどちらか。**推測で寄せない** |
| **Q9** | **選択肢マスタに無い 9 語**をどうするか | production には `その他` がある。①`その他` へ寄せる ②語をそのまま送る (production は値を検証しないので通る) ③要確認。**②は問診アプリなら起こり得ない値を納品することになる** |
| **Q10** | `BA`〜`BI` (がんリスク検査・AI疾病予測の設問) | production が**意図的に削除した設問** (CLAUDE.md「がんリスク = 検査機関から取得」)。**納品に入れない**が既定。入れるなら別途判断 |

**`G` `H` `I` は判断不要** — `interview/export` が `dateOfBirth` / `sex` を引数で受ける
(`export.ts:13-18`) ので、**answers ではなくパラメータへ回す**。`G` 氏名は**納品に入れない**
(既存仕様どおり PII をパスにも JSON にも載せない)。

**`M-PERIOD` / `M-FREQ` の `摂取していない` は判断不要** — `M-HAS` が「ない」のとき
production は `when` で**その設問自体を出さない** (`interview-script.ts:469-481`)。
→ **送らないのが正しい**。§5.3 #4 の `resolvePath` 一致確認がそのまま検出する。**実例が出た。**

### 15.6 ZIP の置き場所

受領した ZIP は**この作業セッションの scratchpad にだけ**ある (リポジトリにコミットしない)。
**セッションが終われば消える。** 実装時に必要になったら再受領する。

> **お願い**: Google Drive の共有は「リンクを知っている全員」になっていた (実測)。
> 作業が済んだら**制限付きに戻すか削除する**。

---

## 16. 問診 変換表 (P-3・今回限りの入力アダプタ)

**2026-09-15 の裁定 Q8 / Q9 / Q10 で、変換規則が 1 本に決まった。**
本節が変換表の**正本**。実装はこの表を別ファイルへ写すだけで、判断を足さない。

### 16.1 値の規則 — 3 段だけ。上から順に当てる

| # | 条件 | 出力 | 根拠 |
|---|---|---|---|
| 1 | 値が production の選択肢と**完全一致** | **そのラベル** | 何もしない |
| 2 | **波ダッシュだけが違う** (§16.3 の 13 行に**実物の文字で載っているものだけ**) | **production のラベル** | §15.3。表に 1 対 1 で書く。**規則で一括置換しない** |
| 3 | それ以外 | **XLSX の値をそのまま** | **裁定 Q8 / Q9** |

**推測しない・寄せない・捨てない。** 3 で出る値は production の問診アプリからは出ない値だが、
**それを承知で「そのまま送る」のが裁定** (発注者 2026-09-15)。**捏造ゼロは守られる**
— アプリが作らない値を送ることはあっても、**元データに無い値を作ることはない**。

**送る形は production の engine と同じ** (§5.1 の表)。複数選択は `;` 区切りを割って `string[]`
(**末尾の `;` は空要素として捨てる**)。`F-FREQ` は 9 列を `formatMatrix()` で 1 本の文字列にする。

### 16.2 列の割り当て — **62 列すべてに行き先がある (未分類 0)**

| 列 | 見出し (先頭) | 行き先 | 規則 | 実測 |
|---|---|---|---|---|
| `A` | ID | —（送らない） | Forms の連番 |  |
| `B` | 開始時刻 | —（送らない） | 開始時刻 |  |
| `C` | 完了時刻 | —（送らない） | 完了時刻 |  |
| `D` | メール | —（送らない） | 全員 'anonymous' |  |
| `E` | 名前 | —（送らない） | 空 |  |
| `F` | 最終変更時刻 | —（送らない） | 空 |  |
| `G` | お名前を教えてください。（氏名） | —（answers 外） | 人物突合にのみ使用・納品しない |  |
| `H` | 生年月日を教えてください。（1980年1月30日の場 | —（answers 外） | dateOfBirth パラメータ |  |
| `I` | 生物学的性別を教えてください。 | —（answers 外） | sex パラメータ |  |
| `J` | 身長を教えてください。（172cmの場合の入力例：1 | B-HEIGHT (text) | 全て素通し | 一致0/素通し7 |
| `K` | 体重を教えてください。※数字のみ（65kgの場合の入 | B-WEIGHT (text) | 全て素通し | 一致0/素通し7 |
| `L` | ご自身の体重変化について、該当するものを教えてくださ | B-WEIGHT-CHANGE (multi) | ラベル一致 | 一致3/素通し0 |
| `M` | 現在気になる自覚症状を教えてください。※複数選択可 | H-SYMPTOMS (list multi) | 一致はラベル / 残りは素通し | 一致7/素通し1 |
| `N` | 現在罹患している疾患を教えてください。※複数選択可 | H-CURRENT (list multi) | 一致はラベル / 残りは素通し | 一致4/素通し1 |
| `O` | 過去に罹患した疾患名を教えてください。※複数選択可 | H-PAST (list multi) | 一致はラベル / 残りは素通し | 一致4/素通し4 |
| `P` | 【がんリスク検査かつ疾患名を選択いただいた方のみご回 | H-TREAT-STATUS (chip) | ラベル一致 | 一致2/素通し0 |
| `Q` | 【がんリスク検査かつ疾患名を選択いただいた方のみご回 | H-TREAT-DETAIL (text) | 全て素通し | 一致0/素通し5 |
| `R` | 喫煙習慣はありますか？ | S-STATUS (chip) | ラベル一致 | 一致2/素通し0 |
| `S` | 喫煙を止めた年齢を教えてください。（30歳の場合の入 | S-QUIT-AGE (chip) | 全て素通し | 一致0/素通し5 |
| `T` | 1日の喫煙本数（喫煙者は現在、禁煙者は過去の本数）を | S-COUNT (chip) | 全て素通し | 一致0/素通し3 |
| `U` | 喫煙している/していた年数を教えてください。（15年 | S-YEARS (chip) | 全て素通し | 一致0/素通し5 |
| `V` | 現在の飲酒習慣はありますか？ | D-FREQ (list) | 一致はラベル / 残りは素通し | 一致3/素通し2 |
| `W` | 何歳まで飲酒されていたか教えてください。（45歳の場 | D-UNTIL-AGE (chip) | ラベル一致 | 一致0/素通し0 |
| `X` | 飲酒している/していた年数を教えてください。（15年 | D-YEARS (chip) | 全て素通し | 一致0/素通し5 |
| `Y` | 飲酒する/していた際の、1回あたりの飲酒量を教えてく | D-AMOUNT (chip) | 一致はラベル / 残りは素通し | 一致1/素通し2 |
| `Z` | 食事について、以下のうち該当するものを教えてください | F-HABITS (list multi) | ラベル一致 | 一致5/素通し0 |
| `AA` | 野菜・海藻類 | F-FREQ の行 | matrix 1 行 |  |
| `AB` | フルーツ | F-FREQ の行 | matrix 1 行 |  |
| `AC` | 魚・海産物 | F-FREQ の行 | matrix 1 行 |  |
| `AD` | 赤身肉・加工肉 | F-FREQ の行 | matrix 1 行 |  |
| `AE` | 揚げ物・脂っこい食事 | F-FREQ の行 | matrix 1 行 |  |
| `AF` | 塩分の多い食事 | F-FREQ の行 | matrix 1 行 |  |
| `AG` | 間食・甘いもの | F-FREQ の行 | matrix 1 行 |  |
| `AH` | カフェイン（コーヒー、エナジードリンクなど） | F-FREQ の行 | matrix 1 行 |  |
| `AI` | ご飯（お米） | F-FREQ の行 | matrix 1 行 |  |
| `AJ` | カフェインの1日あたりの摂取量について教えてください | F-CAFFEINE (chip) | 全て素通し | 一致0/素通し7 |
| `AK` | 1回あたりのご飯（お米）の量を教えてください。（約1 | F-RICE (chip) | 全て素通し | 一致0/素通し6 |
| `AL` | 1回あたりの野菜摂取量に対して、ご自身のお考えを教え | F-VEG (chip) | ラベル一致 | 一致3/素通し0 |
| `AM` | ダイエットのための食事制限について教えてください。 | F-DIET-RESTRICT (chip) | ラベル一致 | 一致2/素通し0 |
| `AN` | 食事方法で意識しているものがあれば教えてください。※ | F-DIET-METHOD (multi) | 一致はラベル / 残りは素通し | 一致2/素通し4 |
| `AO` | 週あたりの運動頻度を教えてください。 | E-FREQ (chip) | 一致はラベル / 残りは素通し | 一致2/素通し2 |
| `AP` | 1回あたりの運動時間を教えてください。 | E-TIME (chip) | 一致はラベル / 残りは素通し | 一致2/素通し2 |
| `AQ` | 同年代の人と比較した歩く速さを教えてください。 | E-SPEED (chip) | ラベル一致 | 一致2/素通し0 |
| `AR` | 1日のうち座りっぱなしの時間を教えてください。（1日 | E-SITTING (chip) | 全て素通し | 一致0/素通し7 |
| `AS` | 主な運動の種類を教えてください。 | E-TYPE (list) | 一致はラベル / 残りは素通し | 一致4/素通し2 |
| `AT` | 現在服用中の薬・定期的に摂取しているサプリメント・健 | M-HAS (chip) | ラベル一致 | 一致2/素通し0 |
| `AU` | 現在服用中の薬・定期的に摂取しているサプリメント・健 | M-NAME (text) | 全て素通し | 一致0/素通し6 |
| `AV` | 現在服用中の薬・定期的に摂取しているサプリメント・健 | M-PERIOD (chip) | 一致はラベル / 残りは素通し | 一致1/素通し2 |
| `AW` | 現在服用中の薬・定期的に摂取しているサプリメント・健 | M-FREQ (chip) | 一致はラベル / 残りは素通し | 一致1/素通し1 |
| `AX` | 平均的な睡眠時間を教えてください。 | SL-HOURS (chip) | 一致はラベル / 残りは素通し | 一致1/素通し2 |
| `AY` | 睡眠の質を教えてください。 | SL-QUALITY (chip) | ラベル一致 | 一致4/素通し0 |
| `AZ` | 仕事・家庭・環境など含む総合的なストレスについて点数 | SL-STRESS (slider) | 全て素通し | 一致0/素通し6 |
| `BA` | 【がんリスク検査】がんリスク検査のユーザーIDを入力 | —（送らない） | Q10 入れない |  |
| `BB` | 【がんリスク検査】採尿した日付と時刻を教えてください | —（送らない） | Q10 |  |
| `BC` | 【がんリスク検査】採尿前日の飲酒について教えてくださ | —（送らない） | Q10 |  |
| `BD` | 【がんリスク検査】採尿前日の薬・サプリメントについて | —（送らない） | Q10 |  |
| `BE` | 【がんリスク検査】ALAカプセルを採取した日付と時刻 | —（送らない） | Q10 |  |
| `BF` | 【がんリスク検査】ALAカプセル採取から採尿までに排 | —（送らない） | Q10 |  |
| `BG` | 【がんリスク検査】排尿回数と時刻を教えてください。（ | —（送らない） | Q10 |  |
| `BH` | 【がんリスク検査】採尿検体を一時冷凍保存したかを教え | —（送らない） | Q10 |  |
| `BI` | 【AI疾病予測】直近1年の体重変化（〇kg）を教えて | —（送らない） | Q10 |  |
| `BJ` | 個人情報の取り扱いについて同意します。 | —（送らない） | 同意設問 (production 削除済) |  |

**数え切った結果: 62 / 62。** 取りこぼし 0 (§5.3 #12)。

### 16.3 波ダッシュ変換表 — **この 13 行だけ**

XLSX は `～` U+FF5E FULLWIDTH TILDE / production は `〜` U+301C WAVE DASH (§15.3)。
**NFKC では一致しない**ので、実物の文字を左に置いて 1 対 1 で書く。

| 設問 | XLSX の値 (U+FF5E) | production のラベル (U+301C) |
|---|---|---|
| `D-FREQ` | `月2～4回飲む` | `月2〜4回飲む` |
| `D-FREQ` | `週2～3日飲む` | `週2〜3日飲む` |
| `D-AMOUNT` | `1～2合` | `1〜2合` |
| `D-AMOUNT` | `2～3合` | `2〜3合` |
| `E-FREQ` | `週1～2日` | `週1〜2日` |
| `E-FREQ` | `週3～4日` | `週3〜4日` |
| `E-TIME` | `30～60分` | `30〜60分` |
| `E-TIME` | `60～90分` | `60〜90分` |
| `M-PERIOD` | `1～3カ月` | `1〜3カ月` |
| `SL-HOURS` | `5～6時間` | `5〜6時間` |
| `SL-HOURS` | `6～7時間` | `6〜7時間` |
| `F-FREQ` の列 | `週2～3回` | `週2〜3回` |
| `F-FREQ` の列 | `週4～5回` | `週4〜5回` |

**選択式 11 行 + `F-FREQ` の列 2 種 = 13 行で全部。**
**一括置換の規則にしない** — 見えない文字の置換を規則で持つと、次にハイフン・中点・全角空白で
同じことが起きたときに際限がなくなる (§5.3 #6)。**表に無い波ダッシュは規則 3 で素通しになる。**

### 16.4 そのまま送る語 (裁定 Q9) — 実測 12 語

production の選択肢マスタに無いが、**そのまま送る**。`その他` へ寄せない。

| 設問 | 値 |
|---|---|
| `H-SYMPTOMS` | `肌のぶつぶつと痒み` |
| `H-CURRENT` | `痛風` |
| `H-PAST` | `卵巣嚢腫` |
| `H-PAST` | `肺炎` |
| `H-PAST` | `脳動静脈奇形` |
| `H-PAST` | `腹膜炎` |
| `F-DIET-METHOD` | `なし` |
| `F-DIET-METHOD` | `揚げ物は食べない` |
| `F-DIET-METHOD` | `特にない` |
| `F-DIET-METHOD` | `特になし` |
| `E-TYPE` | `ゴルフ` |
| `E-TYPE` | `街歩き程度` |

**`M-PERIOD` / `M-FREQ` の `摂取していない` は送らない。** `M-HAS` が「ない」のとき production は
`when` でその設問自体を出さない (`interview-script.ts:469-481`) ため、**設問ごと落とすのが正しい**。
§5.3 #4 の `resolvePath` 一致確認がこれを検出する。

### 16.5 そのまま送る値 (裁定 Q8 と同じ扱い)

| 設問 | production の型 | XLSX の実測 | 出力 |
|---|---|---|---|
| `S-QUIT-AGE` | chip (`30歳未満`…) | 数値 5 種 | **数値のまま** |
| `S-COUNT` | chip (`5本未満`…) | 数値 3 種 | **数値のまま** |
| `S-YEARS` | chip (`5年未満`…) | 数値 5 種 | **数値のまま** |
| `D-YEARS` | chip (`5年未満`…) | 数値 5 種 | **数値のまま** |
| `E-SITTING` | chip (`3時間未満`…) | 数値・`8時間以上`・全角 `８` が混在 7 種 | **そのまま** |
| `F-CAFFEINE` | chip (`1日1〜2杯`…) | 自由記述 7 種 | **そのまま (裁定 Q8)** |
| `F-RICE` | chip (`茶碗1杯（約150g）`…) | 自由記述 6 種 | **そのまま (裁定 Q8)** |

**当初案の「数値 → 区間へ決定論で落とす」は採らない。** 裁定 Q8 / Q9 が示した原則
(**元の値を保つ・production の語彙へ強制的に寄せない**) を、同じ性質の列にも同じく当てる。
区間へ丸めると `25` が `30歳未満` になり**元の精度が失われる**。
> **別扱いにしたい場合はここだけ差し戻せる** — 区間へ落とす規則は決定論で書けるので、
> `S` `T` `U` `X` の 4 列だけ後から切り替えられる。

### 16.6 answers に入れないもの

| 列 | 扱い | 理由 |
|---|---|---|
| `G` 氏名 | **人物の突合にだけ使う。納品しない** | PII をパスにも JSON にも載せない (既存仕様) |
| `H` 生年月日 | `interview/export` の **`dateOfBirth` パラメータ** (`export.ts:13-18`) | 年齢算出にのみ使われ**保存されない** |
| `I` 生物学的性別 | 同 **`sex` パラメータ** | `subject.sex` になる |
| `A`〜`F` | 送らない | Forms の連番・時刻・`anonymous`・空 |
| `BA`〜`BI` | **送らない (裁定 Q10)** | production が意図的に削除した設問。がんリスク検査は検査機関から取得する (CLAUDE.md) |
| `BJ` 同意 | 送らない | production 削除済 (登録/オンボーディングで取得する範疇) |

### 16.7 実装のときに必ず確かめる (§5.3 #12 / #13)

1. **62 / 62 の行き先が表にあること**を件数で示す (§16.2)。
2. **8 名ぶんを `resolvePath(answers)` に通し、生成した id 集合と一致すること。**
   `M-HAS`=ない の人で `M-NAME` / `M-PERIOD` / `M-FREQ` が落ちることが実データで確認できる。
3. **XLSX の生の値 → 送る値**の対照を 10 名ぶん画面に出し、人が 1 回通して見る。
4. 分岐を決める 4 設問 (`S-STATUS` `D-FREQ` `E-FREQ` `M-HAS`) が
   **production のラベルに解決していること** — 実測でこの 4 つは規則 1 か 2 で必ず一致する
   (ここが素通しになると `resolvePath` が狂う)。

### 16.8 実装中に見つけた production の癖 (直さない)

`SL-STRESS` は engine が `"9 / 10"` という文字列を書き (`live-controller.ts:431`)、
`formatAnswerLabel` がそこへもう一度 `/ 10` を足すので **`answer_label` が `"9 / 10 / 10"` になる**
(`interview-script.ts:704-706`)。**production の実挙動なので、アダプタも `"N / 10"` を送って揃える。**
**ここで直さない** (production の挙動を V4 の都合で変えない・§11.3)。
