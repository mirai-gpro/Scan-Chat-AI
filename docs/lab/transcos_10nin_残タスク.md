# トランスコスモス10名 ― 残タスク

作成: 2026-09-15 / 状態: **着手していない。発注者の指示があるまで実装しない**
仕様の正本: `docs/lab/transcos_10nin_v4_spec.md` (v4.0)
ブランチ: `design/transcos-zero-base` (Scan-Chat-AI / wellfort-site)

> **この文書は「何が残っているか」だけを書く。** 仕様・設計・変換表は書かない
> (v4 仕様書が正本。ここに写すと二重管理になる)。

---

## 0. いまの状態

| | |
|---|---|
| V1 / V2 / V3 | **全廃案。撤回済み** (Scan-Chat-AI `7d30669` / wellfort-site `146fa46`) |
| V4 の設計 | **完了** (v4 仕様書 §0〜§16) |
| 発注者裁定 | **Q1〜Q4 / Q7〜Q10 が済み** (仕様書 §13.1) |
| 実物 ZIP | **受領して全ファイルを数え終えた** (§15)。**ただしセッション終了で消えた**ので再受領が要る |
| 問診の変換表 | **確定** (§16)。62 / 62 列に行き先があり未分類 0 |
| 実装 | **0 行。1 ファイルも作っていない** |

**残るのは「作ること」と、発注者側の判断 2 件 (Q5 / Q6) と、Elith への申し送りだけ。**

---

## 1. 着手前に要るもの (実装のブロッカー)

| # | 要るもの | 誰が | 備考 |
|---|---|---|---|
| **B-1** | **実装の着手承認** | 発注者 | 仕様書 §16 の STOP をまだ解除していない |
| **B-2** | **ZIP の再受領** | 発注者 | 前回の Google Drive リンクは**この作業環境から消えている**。実装時にもう一度要る |

**B-2 の注意**: 前回の共有は「リンクを知っている全員」だった (実測)。
**作業が済んだら制限付きへ戻すか削除する。**

---

## 2. 実装タスク (v4 仕様書 §14 の順序)

| # | 内容 | 前提 | 規模 |
|---|---|---|---|
| **T-1** | 「臨時診断バッチ」画面 — ZIP 選択 / 人物分離 / 「最初の 1 名だけ」 (= Test A・B) | B-1 B-2 | wellfort-site 1 ページ + 2 ファイル |
| **T-2** | 呼び出し順の制御 (人物 × 検査 × ページ)・進捗保持 (IndexedDB)・再開 | T-1 | 2 ファイル |
| **T-3** | 問診アダプタ — §16 の表を**別ファイルへ写す**。`resolvePath` 一致確認と対照表示 | T-1 | 2 ファイル (アダプタ / 変換表) |
| **T-4** | `/api/interview/export` への中継 (wellfort-site) | T-3 | 1 ファイル (既存 86 行と同型) |
| **T-5** | Test C — **通常経路と同一であることの証明** (再実装 0 件を機械で見る) | T-1 | 検証 |
| **T-6** | 10 名を通す (Test D / E) + 結果表 + 対応表 CSV | T-2 T-3 T-4 | — |
| **T-7** | Test F — 同じ ZIP を 2 回流してもファイルが増えない (冪等) | T-6 | 検証 |
| **T-8** | Elith へ渡す → Test G (先方で正常処理) | T-6 T-7 | 先方待ち |

**合計 7 ファイル + 1 ページ** (仕様書 §11.1)。**Scan-Chat-AI は 1 行も変えない。**

### 2.1 T-3 / T-6 で必ず確かめる 4 点 (§16.7)

1. **62 / 62 の行き先が表にあること**を件数で示す
2. **8 名を `resolvePath(answers)` に通し、生成した id 集合と一致すること**
   (`M-HAS`=ない の人で `M-NAME` / `M-PERIOD` / `M-FREQ` が落ちるのが実データで見える)
3. **XLSX の生の値 → 送る値**の対照を 10 名ぶん画面に出し、人が 1 回通して見る
4. 分岐を決める 4 設問 (`S-STATUS` `D-FREQ` `E-FREQ` `M-HAS`) が
   **production のラベルに解決していること** (素通しになると `resolvePath` が狂う)

### 2.2 手作業が残るところ (自動化しない)

- **紙の問診 2 名分**は手入力 (神谷様 5 ページ / 古原様 10 ページ・どちらもテキスト PDF)。
  **パーサを作らない** (§5.3 #9)。
- 分類できないファイル / 変換表に無い列・値が出たら、**その人物だけ**を要確認にして
  残りは最後まで通す (§4.4)。

---

## 3. 発注者の判断待ち (実装は止めない)

| # | 内容 | 急ぐか |
|---|---|---|
| **Q5** | 撤回した DB (6 テーブル + `executive_subjects`) を実際に DROP するか。**`executive_subjects` は email の unique index を持ち PII を含む**。ロールバック SQL の案は提出済み・**適用していない** | **PII が残り続けるので、実装とは別に早めに** |
| **Q6** | S3 の後片付け。`ad-hoc-uploads/` は案件専用で安全。**E2E が書いた納品 JSON 1 件は本番の納品と同じ名前空間**にあるので個別に特定が要る。**削除候補の提示のみ・実削除はしていない** | 実装前でも後でもよい |
| **Q3 の残り** | 10 名が**アプリで結果を見るか**。見るなら、採番した uuid で `diagnosis.app_users` に行を作る (production DB への書き込み = 別途承認) | **納品の前でなくてよい**。uuid なので後から載せられる |
| **Q8 の差し戻し** | `S` `T` `U` `X` (喫煙を止めた年齢 / 本数 / 年数 / 飲酒年数) を**数値のまま**送る判断を、**区間の選択肢へ丸める**に変えるか。4 列だけ後から切り替えられる (§16.5) | Elith の回答 (下記) 次第 |

---

## 4. Elith への申し送り

**文面は作成済み** = `docs/elith/transcos_10nin_elith申し送り.md`。**未送付。**

送ったあと、**先方の回答で仕様が動きうるのは 1 点だけ**:

- **§2「問診に選択肢以外の値が入る」** — 先方の解析が選択肢の固定値を前提にしている場合、
  **区間へ丸めた形に切り替える**選択肢を提示してある (= 上の「Q8 の差し戻し」)。

他の 3 点 (client_id の新規採番 / がんリスク設問を含めない / 納品範囲 3 種類) は
**確認であって、回答待ちで実装が止まるものではない。**

あわせて確認しておきたいこと:

- **書き出し完了の知らせ方** — フォルダの存在で自動処理が始まる運用ではない、という理解でよいか
  (`elith_s3_data_handoff_spec §8.2` の「起動トリガ」は**要確認のまま**)。

---

## 5. やらないと決めたこと (蒸し返さない)

| | 理由 |
|---|---|
| 新しい診断 pipeline / JSON schema / 独自 parser / 独自 canonical table | 仕様書 §12。**既存 Production をそのまま呼ぶ**のが本案件の前提 |
| 紙の問診 PDF のパーサ | §5.3 #9。2 名なので手入力 |
| 問診アダプタの汎用化・他案件への使い回し | §5.3.1。**完全オーダーメイド** (発注者裁定) |
| 波ダッシュを正規化規則で一括置換する | §16.3。表に 13 行を verbatim で持つ。規則にすると際限がなくなる |
| `SL-STRESS` の `answer_label` が `"9 / 10 / 10"` になる production の癖を直す | §16.8。**V4 の都合で production の挙動を変えない** |
| 解析フェーズのドライラン | §6.2。ワンクリック納品と**技術的に両立しない** (`AWS_REGION` はサーバ全体の env) |
| DB テーブルの新設 | §7.2。進捗はブラウザの IndexedDB |
| 健診 B の構造化 XLSX を納品経路にする | §15.2 F3。**読み取りの照合 (5/10 名) にだけ使う** |

---

## 6. 着手するときの入口

1. この文書で残りを確認する
2. **仕様の正本** `docs/lab/transcos_10nin_v4_spec.md` を読む
   (§4 画面 / §5 問診の枠 / §15 実測 / §16 変換表)
3. ZIP を再受領する (B-2)
4. T-1 から順に進める。**Scan-Chat-AI の `src/` は 1 行も変えない**
